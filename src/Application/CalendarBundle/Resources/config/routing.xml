<?xml version="1.0" encoding="UTF-8" ?>

<routes xmlns="http://www.symfony-project.org/schema/routing"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.symfony-project.org/schema/routing http://www.symfony-project.org/schema/routing/routing-1.0.xsd">

    <route id="calendar" pattern="/calendar">
        <default key="_controller">CalendarBundle:Calendar:index</default>
    </route>

    <route id="calendar_datafeed" pattern="/calendar/feed">
        <default key="_controller">CalendarBundle:Calendar:feed</default>
    </route>

    <route id="calendar_add" pattern="/calendar/add">
        <default key="_controller">CalendarBundle:Calendar:add</default>
    </route>

    <route id="calendar_update" pattern="/calendar/update">
        <default key="_controller">CalendarBundle:Calendar:update</default>
    </route>

    <route id="calendar_edit" pattern="/calendar/edit">
        <default key="_controller">CalendarBundle:Calendar:edit</default>
    </route>

    <route id="calendar_updateDetails" pattern="/calendar/updateDetails">
        <default key="_controller">CalendarBundle:Calendar:updateDetails</default>
    </route>

    <route id="calendar_delete" pattern="/calendar/delete">
        <default key="_controller">CalendarBundle:Calendar:delete</default>
    </route>
</routes>